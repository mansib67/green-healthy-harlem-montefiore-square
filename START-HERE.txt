Upload every file from this folder into the EMPTY repository root.
After that, open the images folder on GitHub and upload the whole gallery folder from the second zip.
Then go to Settings > Pages > Deploy from a branch > main > /(root).
